import {useEffect,useState} from 'react';import {Menu,X,ArrowUpRight} from 'lucide-react';import {motion} from 'framer-motion';import {navItems,profile} from '../../config/profile';import {useActiveSection} from '../../hooks/useActiveSection';
export function Navbar(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);const active=useActiveSection(navItems.map(x=>x[1]));useEffect(()=>{const on=()=>setScrolled(window.scrollY>24);window.addEventListener('scroll',on,{passive:true});return()=>window.removeEventListener('scroll',on)},[]);useEffect(()=>{const on=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false);window.addEventListener('keydown',on);return()=>window.removeEventListener('keydown',on)},[]);const go=(id:string)=>{document.getElementById(id)?.scrollIntoView({behavior:'smooth'});setOpen(false)};return <header className="fixed top-0 left-0 right-0 z-50 px-3 sm:px-5 pt-3"><nav className={`mx-auto max-w-6xl border transition-all duration-300 ${scrolled?'rounded-2xl border-slate-200/90 bg-white/90 shadow-soft':'rounded-3xl border-white/70 bg-white/65'} backdrop-blur-xl px-3 sm:px-4`} aria-label="Navigasi utama"><div className={`flex items-center justify-between transition-all ${scrolled?'py-2':'py-2.5'}`}><button onClick={()=>go('home')} className="flex items-center gap-2.5 font-bold tracking-tight text-ink" aria-label="Kembali ke beranda"><span className="grid h-9 w-9 place-items-center rounded-xl bg-ink text-white text-xs">PP</span><span className="hidden sm:inline">Portfolio<span className="text-accent">.</span></span></button><div className="hidden lg:flex items-center gap-1">{navItems.map(([label,id])=><button key={id} onClick={()=>go(id)} className={`rounded-xl px-3 py-2 text-sm transition ${active===id?'bg-paper text-accent font-semibold':'text-muted hover:text-ink'}`}>{label}</button>)}</div><button onClick={()=>go('contact')} className="hidden lg:inline-flex items-center gap-1.5 rounded-xl bg-accent px-3.5 py-2 text-xs font-bold text-white hover:-translate-y-0.5 transition">Kontak <ArrowUpRight size={14}/></button><button className="lg:hidden p-2 rounded-xl" onClick={()=>setOpen(!open)} aria-label={open?'Tutup menu':'Buka menu'} aria-expanded={open}>{open?<X size={20}/>:<Menu size={20}/>}</button></div>{open&&<motion.div initial={{opacity:0,height:0}} animate={{opacity:1,height:'auto'}} className="lg:hidden border-t border-slate-200/70 pt-2 pb-3 grid gap-1">{navItems.map(([label,id])=><button key={id} onClick={()=>go(id)} className="text-left rounded-xl px-3 py-3 hover:bg-paper">{label}</button>)}<button onClick={()=>go('contact')} className="mt-1 rounded-xl bg-ink px-3 py-3 text-left font-semibold text-white">Hubungi {profile.name}</button></motion.div>}</nav></header>}
