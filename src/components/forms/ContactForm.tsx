import {useState} from 'react';
import type {FormEvent} from 'react';import type {ContactSubmission} from '../../types';import {CheckCircle2,Loader2} from 'lucide-react';const initial:ContactSubmission={name:'',email:'',subject:'',message:''};export function ContactForm(){const [form,setForm]=useState(initial);const [status,setStatus]=useState<'idle'|'loading'|'success'|'error'>('idle');const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(!form.name||!form.email||!form.subject||!form.message){setError('Semua field wajib diisi.');return}if(!/^\S+@\S+\.\S+$/.test(form.email)){setError('Masukkan alamat email yang valid.');return}setStatus('loading');try{const endpoint=import.meta.env.VITE_CONTACT_ENDPOINT as string|undefined;if(endpoint){const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!r.ok)throw new Error('Submission failed')}else await new Promise(r=>setTimeout(r,600));setStatus('success');setForm(initial)}catch{setStatus('error');setError('Pesan gagal dikirim. Periksa endpoint kontak atau coba lagi.')}};return <form onSubmit={submit} className="card space-y-5" noValidate>{status==='success'?<div className="py-10 text-center"><CheckCircle2 className="mx-auto text-accent" size={40}/><h3 className="mt-4 text-xl font-semibold">Pesan siap diproses</h3><p className="mt-2 text-sm text-muted">Demo form aktif. Hubungkan VITE_CONTACT_ENDPOINT ke layanan pilihan untuk pengiriman nyata.</p><button type="button" className="btn-secondary mt-5" onClick={()=>setStatus('idle')}>Kirim pesan lain</button></div>:<><div className="grid md:grid-cols-2 gap-5"><Field label="Nama" value={form.name} onChange={v=>setForm({...form,name:v})}/><Field label="Email" type="email" value={form.email} onChange={v=>setForm({...form,email:v})}/></div><Field label="Subject" value={form.subject} onChange={v=>setForm({...form,subject:v})}/><label className="block"><span className="label">Message</span><textarea rows={6} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} className="input resize-y"/></label>{error&&<p className="text-sm text-red-600" role="alert">{error}</p>}{status==='error'&&<p className="text-sm text-red-600" role="alert">Submission error.</p>}<button disabled={status==='loading'} className="btn-primary">{status==='loading'?<><Loader2 className="animate-spin" size={17}/> Mengirim…</>:'Kirim pesan'}</button></>}</form>}
function Field({label,type='text',value,onChange}:{label:string;type?:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="label">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="input"/></label>}
