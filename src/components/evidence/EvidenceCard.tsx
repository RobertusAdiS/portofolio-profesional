import {ExternalLink,FileText,Image as ImageIcon,Video} from 'lucide-react';import type {Evidence} from '../../types';const icons={document:FileText,image:ImageIcon,video:Video,external:ExternalLink};export function EvidenceCard({item,onOpen}:{item:Evidence;onOpen:(x:Evidence)=>void}){const Icon=icons[item.type];return <article className="card"><div className="flex justify-between"><div className="h-10 w-10 rounded-xl bg-paper grid place-items-center text-accent"><Icon size={18}/></div>{item.status==='placeholder'&&<span className="tag">Placeholder</span>}</div><p className="mt-5 text-xs font-semibold uppercase tracking-wider text-accent">{item.category}</p><h3 className="mt-2 font-semibold text-ink">{item.title}</h3><p className="mt-2 text-sm leading-6 text-muted">{item.description}</p><button onClick={()=>onOpen(item)} className="mt-5 text-sm font-semibold text-accent">Lihat detail →</button></article>}
