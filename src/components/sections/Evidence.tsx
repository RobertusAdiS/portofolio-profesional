import {useState} from 'react';import {evidence} from '../../data/evidence';import type {Evidence as EvidenceType} from '../../types';import {EvidenceCard} from '../evidence/EvidenceCard';import {EvidenceModal} from '../evidence/EvidenceModal';import {SectionHeading} from '../ui/SectionHeading';export function Evidence(){const [selected,setSelected]=useState<EvidenceType|null>(null);return <section id="artifacts" className="section bg-white"><div className="max-w-6xl mx-auto px-5"><SectionHeading eyebrow="Artifacts" title="Bukti belajar yang dapat diverifikasi." description="Koleksi artifact menggunakan placeholder sampai file dan URL asli tersedia."/><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5">{evidence.map(x=><EvidenceCard key={x.id} item={x} onOpen={setSelected}/>)}</div></div><EvidenceModal item={selected} onClose={()=>setSelected(null)}/></section>}

